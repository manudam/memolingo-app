# This is a template file. Rename it to key.properties and fill in the values.
# Store this file outside of version control for security reasons.

storePassword=your_keystore_password
keyPassword=your_key_password
keyAlias=upload
storeFile=../app/upload-keystore.jks
